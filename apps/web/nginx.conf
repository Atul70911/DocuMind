server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # This is critical — without it, refreshing on /chat/123 gives 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}